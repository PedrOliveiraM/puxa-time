import { StyleSheet } from 'react-native'

export const s = StyleSheet.create({
  button: {
    padding: 15,
  },
  container: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    width: '100%',
    paddingTop: 40,
    padding: 10,
  },
})
